const users = {
    
}

module.exports = {
    users: [
        { name: 'Beki Jr', password: 'hopedieshere'},
        { name: 'Zele', password: 'nigger'}
    ]
}


// const found = () => users.find(usr => usr.name === user_id)
